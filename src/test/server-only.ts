// Test stub for the `server-only` package. In a real build `server-only`
// throws if it ends up in a client bundle; under vitest (plain Node) that
// guard has no meaning, so importing modules that use it should be a no-op.
export {};
